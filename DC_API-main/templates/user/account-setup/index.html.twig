{% extends "base.html.twig" %}

{% block body %}
{{ form_start(form) }}
<div class="mx-auto max-w-screen-xl px-4 py-16 sm:px-6 lg:px-8">
  <div class="mx-auto max-w-lg text-center">
    <h1 class="text-2xl font-bold sm:text-3xl">Encore une dernière étape</h1>

    <p class="mt-4 text-gray-500">
      Il vous reste encore quelques informations que vous pouvez remplir.
    </p>
  </div>

  <div class="mx-auto mb-0 mt-8 max-w-md space-y-4">
   {{ form_widget(form.profile_picture)}}    
   {{ form_widget(form.school) }}    
    <div class="flex items-center justify-between">
      {{ form_widget(form.valider) }}
    </div>
  </div>
</div>
{{ form_end(form) }}
{% endblock %}
